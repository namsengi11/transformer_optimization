---
description: Boot or resume the serial recursive transformer optimization loop.
argument-hint: "[number-of-steps]"
allowed-tools: Read, Grep, Glob, Bash, Edit, Write, Task
---

Read and follow `agent/commands/optimize.md`. Treat `$ARGUMENTS` as its optional step limit.
Store every agent-owned file and result beneath `agent/` as required by `agent/README.md`.
