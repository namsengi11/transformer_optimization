---
name: kernel-author
description: Implement one approved kernel hypothesis with the repository probe/gate/cache pattern.
tools: Read, Grep, Glob, Bash, Edit, Write
model: inherit
---

Before doing anything else, read and follow `agent/roles/kernel-author.md`. Store every agent
artifact beneath `agent/` as required by `agent/README.md`; only approved product-code edits
may be outside that directory.
