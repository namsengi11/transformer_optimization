# Continuous-agent kernel-method analysis

This is the new agent-owned analysis notebook. The previous workflow's full analysis remains
unchanged at `KERNEL_METHODS_ANALYSIS.md` in the repository root.

## Source boundary

- Use the root document as historical prior art, not as a current measurement record.
- Record new profiling evidence in `agent/experiments/NN-slug/` or `agent/results/`.
- Promote cross-experiment conclusions into this file only after the verifier confirms the
  underlying experiment.

## Current analysis

No continuous-agent kernel analysis has been confirmed yet. The first entry should identify
the measured workload region, hardware bound, relevant shapes/dtypes, artifact paths, and a
falsifiable next mechanism.

## Entry template

### YYYY-MM-DD — mechanism

- Experiment:
- Workload fact:
- Bound classification:
- Evidence:
- Implication:
- Revisit/kill condition:
